# TAFND
This is an anonymized repository in which all information potentially revealing the authors’ identities has been masked using placeholder strings such as “XXXXX”.

# Dependencies
The virtual environment and its associated dependencies used in the experiments can be inspected in detail through the provided `environment.yml` file. This file was generated from the original environment using the `conda env export > environment.yml` command, and the environment can be fully reconstructed by executing `conda env create -f environment.yml`.
